rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write for all documents during development
    // WARNING: Upgrade these rules before production!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
